name: list_branches
runner_type: python-script
description: List all branches for a given repository
enabled: true
entry_point: list_branches.py
parameters:
  repo:
    type: string
    description: Name of the repository to list the branches for.
    required: true
