---
username: username
password: password
email: test@test.local

sensor:
  bitbucket_type: server
  targets:
    - repository: 'foo/bar'
      branches:
        - 'master'
    - repository: 'hoge/fuga'
      branches:
        - 'master'
        - 'dev'
  timeout: 20
